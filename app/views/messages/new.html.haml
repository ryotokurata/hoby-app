.wrapper
  = render "groups/header"
  .main
    .content
      .content__main
        .container
          %h2.registration
            投稿
          .registration-form
            = form_for [@group, @message], method: :post,url:group_messages_path(@group.id) do |f|
              .form-contents
                .formblock1
                  .form-group
                    %label{for: "content"} 
                    = f.text_field :content, class:'input-default', placeholder: '題名'
                    %span.require-message 必須
                  .form__mask
                    = f.label :image, class: 'form__mask__image' do
                      = icon('fas', 'image', class: 'icon')
                      = f.file_field :image, class: 'hidden'
                .formblock2
                  .form-group
                    %label{for: "text"} 
                    = f.text_area :text, class:'input-default2', placeholder: '説明'
                    %span.require-message 必須
                  .form-group 
                  =f.submit "投稿" ,class:"btn-red", type:"submit"
  .footer
